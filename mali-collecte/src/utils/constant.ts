import { environment } from 'src/environments/environment';

export const URL_LOGIN: string = `${environment.apiBaseURL}<http://localhost:8080/api/auth/signin>`;
export const URL_REGISTER: string = `${environment.apiBaseURL}<http://localhost:8080/api/auth/signup>`;
